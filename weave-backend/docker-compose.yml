services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: weave
      POSTGRES_USER: weave
      POSTGRES_PASSWORD: weave
    ports:
      - "5432:5432"
    volumes:
      - weave-postgres:/var/lib/postgresql/data

volumes:
  weave-postgres:
