import { Card, Pill, ButtonLink, StatusBadge } from "../../../../components/ui";
import { SurfacePage, Timeline } from "../../../../components/surface";
export default async function EditorRequestDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <SurfacePage role="editor" title={`Request ${id}`} eyebrow="Delivery workspace" action="Back to requests" actionHref="/editor/requests"><div className="grid gap-8 lg:grid-cols-[1.2fr_.8fr]"><div className="space-y-6"><Card><div className="flex justify-between"><h2 className="text-xl font-black">Upload a preview</h2><Pill tone="lime">Revision 0 / 3</Pill></div><div className="mt-5 flex aspect-video items-center justify-center rounded-2xl bg-[var(--ink)] text-5xl text-[var(--accent)]">✦</div><button className="mt-5 w-full rounded-xl border-2 border-dashed border-[var(--line)] py-5 text-sm font-bold">Choose a watermarked file</button></Card><Card><h2 className="text-xl font-black">Request timeline</h2><div className="mt-6"><Timeline items={["Request received", "Brief reviewed", "Preview delivered"]} /></div></Card></div><Card className="h-fit"><StatusBadge status="Payment pending" /><h2 className="mt-4 text-2xl font-black">Deliver with clarity</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">Final assets remain gated until payment status is confirmed. Suspension behavior is not automated; its trigger is pending founder sign-off.</p><div className="mt-6"><ButtonLink href="/editor/earnings" variant="outline">View payment status</ButtonLink></div></Card></div></SurfacePage>; }
