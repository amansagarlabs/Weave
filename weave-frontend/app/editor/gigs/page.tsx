import { Card, ButtonLink } from "../../../components/ui";
import { SurfacePage } from "../../../components/surface";
export default function EditorGigs() { return <SurfacePage role="editor" title="Your editing gigs." action="Add a gig" actionHref="/editor/gigs/new" description="A simple menu helps creators choose the right level of support."><div className="grid gap-5 md:grid-cols-3">{["Basic", "Standard", "Premium"].map((tier, index) => <Card key={tier} className={index === 1 ? "border-2 border-[var(--forest)]" : ""}><p className="text-xs font-bold uppercase tracking-widest text-[var(--muted)]">{tier}</p><h2 className="mt-4 text-2xl font-black">Reel edit</h2><p className="mt-6 text-3xl font-black">₹{[800, 1500, 2800][index]}</p><p className="mt-2 text-sm leading-6 text-[var(--muted)]">Color, cuts, captions, and {index + 1} revision{index ? "s" : ""}.</p><div className="mt-6"><ButtonLink href="/editor/gigs/edit" variant={index === 1 ? "accent" : "outline"}>Edit gig</ButtonLink></div></Card>)}</div></SurfacePage>; }
