import { Card, ButtonLink, Pill, StatusBadge } from "../../../../components/ui";
import { SurfacePage, Timeline } from "../../../../components/surface";
export default async function EditorRequestDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <SurfacePage role="creator" title={`Edit request ${id}`} eyebrow="Editor delivery"><div className="grid gap-8 lg:grid-cols-[1.2fr_.8fr]"><div className="space-y-6"><Card><div className="flex items-center justify-between"><h2 className="text-xl font-black">Preview workspace</h2><Pill tone="lime">Watermarked preview</Pill></div><div className="mt-5 flex aspect-video items-center justify-center rounded-2xl bg-[var(--ink)] text-5xl text-[var(--accent)]">✦</div><p className="mt-4 text-sm leading-6 text-[var(--muted)]">Watermarking and low-resolution previews are best-effort. Weave cannot technically prevent screenshots or screen recordings.</p></Card><Card><h2 className="text-xl font-black">Revision timeline</h2><div className="mt-6"><Timeline items={["Request created", "Preview delivered", "Revision 0 of 3"]} /></div></Card></div><Card className="h-fit"><StatusBadge status="Payment pending" /><h2 className="mt-4 text-2xl font-black">Final asset is gated</h2><p className="mt-3 text-sm leading-6 text-[var(--muted)]">Use the payment link to confirm payment. The final download becomes available after the server verifies the payment status.</p><div className="mt-6"><ButtonLink href="/creator/earnings" variant="accent">View payment status ↗</ButtonLink></div></Card></div></SurfacePage>; }
