# Leave blank for LAN development: the browser will use the host serving the frontend.
# Set explicitly in production, for example https://api.example.com.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_REALTIME_URL=
