"use client";

import { useEffect, useState } from "react";
import { api } from "../lib/api";
import { ButtonLink, Card, Pill, StatusBadge } from "./ui";
import { PublicNav } from "./public-nav";

type Profile = { userId: number; displayName: string; publicSlug: string; categoriesJson: string | null; platformsJson: string | null; city: string | null; contentLanguage: string | null; availabilityStatus: string | null; influencingScore: number | null };
type Package = { id: number; contentType: string; price: number; deliveryDays: number; revisionsIncluded: number; active: boolean };
function list(value: string | null) { try { const parsed = JSON.parse(value || "[]"); return Array.isArray(parsed) ? parsed.map(String) : []; } catch { return []; } }

export function PublicCreatorStorefront({ slug, variant = "brand" }: { slug: string; variant?: "brand" | "creator" }) {
  const [profile, setProfile] = useState<Profile | null>(null); const [packages, setPackages] = useState<Package[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false);
  useEffect(() => { Promise.all([api<Profile>(`/creator/public/${encodeURIComponent(slug)}`), api<Package[]>(`/creator/public/${encodeURIComponent(slug)}/packages`)]).then(([loadedProfile, loadedPackages]) => { setProfile(loadedProfile); setPackages(loadedPackages); }).catch(() => setError("This creator profile could not be loaded right now.")).finally(() => setLoading(false)); }, [slug]);
  async function contact() { if (!profile) return; setBusy(true); setMessage(""); try { await api("/messages", { method: "POST", body: JSON.stringify({ threadId: `creator-${profile.userId}`, recipientId: profile.userId, body: "Hi! I found your profile on Weave and would love to share a brief." }) }); setMessage("Message sent. Open Messages to continue."); } catch (caught) { setMessage(caught instanceof Error ? `${caught.message}. Sign in before messaging.` : "Sign in before messaging."); } finally { setBusy(false); } }
  async function request(pkg: Package) { if (!profile) return; setBusy(true); setMessage(""); try { await api("/bookings", { method: "POST", body: JSON.stringify({ creatorId: profile.userId, packageId: pkg.id, amount: pkg.price }) }); setMessage("Booking request sent. Track it from Bookings."); } catch (caught) { setMessage(caught instanceof Error ? `${caught.message}. Sign in as a brand to request a package.` : "Sign in as a brand to request a package."); } finally { setBusy(false); } }
  return <div className="min-h-screen bg-[var(--paper)]"><PublicNav /><div className="mx-auto max-w-6xl px-6 pb-20 lg:px-8">{loading ? <Card className="mt-10"><p className="font-bold text-[var(--muted)]">Loading creator profile...</p></Card> : error || !profile ? <Card className="mt-10"><h1 className="text-2xl font-black">Creator profile unavailable.</h1><p className="mt-3 text-sm text-[var(--muted)]">{error || "This profile does not exist."}</p><div className="mt-5"><ButtonLink href={variant === "brand" ? "/brand/discover" : "/"} variant="outline">Go back</ButtonLink></div></Card> : <><section className="mt-8 grid gap-8 rounded-3xl bg-[var(--forest)] p-8 text-white md:grid-cols-[auto_1fr_auto] md:items-end"><div className="flex h-28 w-28 items-center justify-center rounded-full bg-[var(--accent)] text-5xl text-[var(--on-bright)]" aria-hidden="true">✦</div><div><Pill tone="lime">{profile.availabilityStatus || "Available for work"}</Pill><h1 className="mt-5 text-5xl font-black tracking-[-.07em]">{profile.displayName}</h1><p className="mt-3 text-white/70">{list(profile.categoriesJson).join(" · ") || "Creator"} · {profile.city || "India"} · @{profile.publicSlug}</p></div>{variant === "brand" ? <button type="button" onClick={contact} disabled={busy} className="min-h-12 rounded-full bg-[var(--accent)] px-5 py-3 text-sm font-bold text-[var(--on-bright)] shadow-[4px_4px_0_var(--ink)]">{busy ? "Sending..." : "Start a conversation →"}</button> : <ButtonLink href="/creator/profile/edit" variant="accent">Edit your public page →</ButtonLink>}</section>{message ? <p role="status" className="mt-5 rounded-2xl bg-[var(--accent)] px-4 py-3 text-sm font-bold text-[var(--on-bright)]">{message}</p> : null}<div className="mt-8 grid gap-8 lg:grid-cols-[1.3fr_.7fr]"><div className="space-y-8"><Card><h2 className="text-2xl font-black">About the work</h2><p className="mt-4 leading-7 text-[var(--muted)]">This public profile is backed by the creator&apos;s saved Weave profile. Audience metrics appear only when the creator has provided them; Weave does not invent performance numbers.</p><div className="mt-6 flex flex-wrap gap-2">{list(profile.categoriesJson).map((category) => <Pill key={category}>{category}</Pill>)}<Pill>{profile.contentLanguage || "Language open"}</Pill></div></Card><Card><div className="flex items-center justify-between"><h2 className="text-2xl font-black">Portfolio</h2><span className="text-sm text-[var(--muted)]">Creator-managed</span></div><div className="mt-6 rounded-2xl border border-dashed border-[var(--line)] p-8 text-center"><p className="font-black">Portfolio media is being prepared.</p><p className="mt-2 text-sm text-[var(--muted)]">Uploaded assets will appear here after S3/R2 storage is connected.</p></div></Card></div><div><h2 className="mb-4 text-2xl font-black">Packages</h2>{packages.length ? <div className="space-y-4">{packages.map((pkg) => <Card key={pkg.id}><h3 className="font-black">{pkg.contentType}</h3><p className="mt-4 text-3xl font-black tabular-nums">₹{pkg.price.toLocaleString("en-IN")}</p><p className="mt-1 text-sm text-[var(--muted)]">{pkg.deliveryDays} day delivery · {pkg.revisionsIncluded} revisions included</p><div className="mt-5">{variant === "brand" ? <button type="button" onClick={() => request(pkg)} disabled={busy} className="min-h-11 rounded-full border-2 border-[var(--ink)] px-4 py-2 text-sm font-bold disabled:opacity-60">{busy ? "Sending..." : "Request package"}</button> : <ButtonLink href="/creator/packages" variant="outline">Manage package</ButtonLink>}</div></Card>)}</div> : <Card><h3 className="font-black">No packages published yet.</h3><p className="mt-2 text-sm text-[var(--muted)]">This creator is still setting up their rate card.</p></Card>}<Card className="mt-4 bg-[var(--ink)] text-white"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--accent)]">Credibility</p><h2 className="mt-3 text-xl font-black">Building a track record</h2><p className="mt-2 text-sm leading-6 text-white/70">Completed-work metrics will appear after enough verified history. We do not manufacture a score.</p></Card></div></div></>}</div></div>;
}
